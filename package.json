{
  "name": "ernesto-kitchen",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5000",
    "dev:versionone": "vite --host 0.0.0.0 --port 5001",
    "build": "vite build",
    "build:versionone": "vite build",
    "preview": "vite preview",
    "preview:versionone": "vite preview --host 0.0.0.0 --port 5001"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "dependencies": {
    "22": "^0.0.0"
  }
}
